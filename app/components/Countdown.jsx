var React = require('react');

var Countdown = React.createClass({
  render:function(){
    return(
      <div>
        <p>This is a countdown page.</p>
      </div>
    );
  }
});

module.exports = Countdown;
